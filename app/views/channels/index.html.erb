<h3 class="title is-2">Channels</h3>

<% if has_role?(:admin) %>
<div class="level">
  <div class="level-left"></div>
  <div class="level-right">
    <%= link_to 'New Channel', new_channel_path,class: "waves-effect waves-light btn" %>
  </div>
</div>

<table class="table is-fullwidth">
  <thead>
    <tr>
      <th>Channel</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @channels.each do |channel| %>
    <tr>
      <td><%= link_to channel.channel, channel %></td>
      <td>
        <div class="buttons is-right">
          <%= link_to 'View Channel', channel, class: "waves-effect waves-light btn" %>
          <%= link_to 'Edit Channel', edit_channel_path(channel), class: "waves-effect waves-light btn" %>
          <%= link_to 'Delete', channel, class: "waves-effect waves-light btn", method: :delete, data: { confirm: "Are you sure you want to delete this channel? " } %>

        </div>
      </td>
    </tr>
    <% end %>
  </tbody>

</table>

<% else %>
 <p class="has-text-center">Sorry! You don't have access this page.</p>
<% end %>
